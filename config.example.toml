# Example configuration for AI Profit Bot.
# Copy to config.toml and adjust. Do not commit real API keys.

symbol = "BTC/USDT"
timeframe = "1h"
initial_cash = 10000
buy_threshold = 0.62
sell_threshold = 0.48
risk_per_trade = 0.01
max_position_fraction = 0.25
stop_loss_pct = 0.03
take_profit_pct = 0.06
fee_rate = 0.001
slippage_rate = 0.0005
